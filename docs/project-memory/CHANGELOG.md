# CHANGELOG

## 2026-09-17 — Phase 10 (Production readiness)
- CHANGE: Completed production-readiness items. (1) Update monitoring (deferred I-04): `ingestion/update-monitor.ts` (`recordToolUpdate`, `latestToolUpdate`) plus a pipeline change-detection branch — when a known source item's `contentHash` differs, the item records a draft `updates` row, increments the run's `updated` metric, marks the ingestion item `updated` and re-baselines the hash, while unchanged repeats are counted as duplicates; the `item_status` enum gained `updated` (migration `0001_lumpy_mysterio.sql`). (2) Admin review for monitored updates: `getToolForAdmin` now returns the tool's recent updates, `setUpdateStatusAction` is a thin server action over the existing `setUpdateStatus` service, and `app/admin/(dashboard)/tools/[id]/page.tsx` renders a "التحديثات المكتشفة" section with Arabic status labels and publish/reject/pending-review forms (no business logic in the UI). (3) Worker observability: `scripts/worker.ts` emits ISO-timestamped level-tagged logs with an optional `--json` line mode and a terminal `run_summary` event (sources/fetched/created/updated/duplicates/invalid/errors/aiProcessed/aiFailed/durationMs), exiting non-zero when any source fails; `summarizeRuns` extended accordingly. (4) Run observability: `/admin/runs` renders the full metrics grid (fetched/created/updated/duplicates/invalid/errors/AI processed vs failed/duration/adapter/finish time) using `metricNumber`/`formatDurationMs`. (5) Backups/ops: `lib/ops/backup.ts` + `scripts/backup.ts` + `npm run db:backup` dump the dev DB inside the container into `backups/` (refusing empty files) and prune to `BACKUP_RETENTION`; new `docs/project-memory/OPERATIONS.md` and a rewritten `README.md` document processes, scheduling, logging, backup/restore and the pre-launch checklist.
- FILES: ingestion/update-monitor.ts, ingestion/pipeline.ts, ingestion/run.ts, lib/services/review.ts (`setUpdateStatus`/`UPDATE_STATUS_TRANSITIONS`), lib/db/queries/admin.ts (updates + `isContentStatus`), lib/actions/tools.ts (`setUpdateStatusAction`), app/admin/(dashboard)/tools/[id]/page.tsx, app/admin/(dashboard)/runs/page.tsx, lib/ui/labels.ts (`metricNumber`/`formatDurationMs`), lib/ops/backup.ts, scripts/{worker,backup}.ts, lib/db/migrations/0001_lumpy_mysterio.sql, lib/db/schema.ts, lib/env.ts (`pgContainer`/`backupDir`/`backupRetention`), package.json (`db:backup`), .env.example, .gitignore, README.md, docs/project-memory/OPERATIONS.md, tests/ingestion/update-monitor.test.ts, tests/ops/backup.test.ts, tests/ui/labels.test.ts, tests/services/review.test.ts, tests/fixtures/rss/update-v{1,2}.xml
- DATABASE IMPACT: `ALTER TYPE "public"."item_status" ADD VALUE 'updated' BEFORE 'ai_processing'` (migration `0001_lumpy_mysterio.sql`, applied to dev; test DB rebuilt by globalSetup). Writes `updates` draft rows on detected changes and `content_revisions` (field `update.status`) on review.
- TEST RESULT: typecheck PASS, eslint PASS, vitest 168/168 PASS (25 files; 21 new tests for update monitoring, backup selection, run-metric formatting and update-status review). Production build PASS (zero warnings). Live verification against the production build: `npm run smoke` 18/18; worker `--json` run emitted timestamped events + a `run_summary` (2 fetched / 2 duplicates, AI-disabled warning, exit 0); logged-in `/admin/runs` 200 with the metrics grid; admin update review drove a draft update to `published` (303 + revision `update.status`/`admin`) and a second to `rejected`, with an unauthorized POST rejected (307 → `/admin/login`, status unchanged); the published update rendered on the public tool page while the rejected one did not; `npm run db:backup` wrote a 55 KiB dump and a scratch restore confirmed 19 tables / 5 tools; anonymous `/admin/runs` still 307 → login. Gemini live call remains PENDING (no `GEMINI_API_KEY`).


## 2026-09-17 — Phase 9 (Testing & hardening)
- CHANGE: Hardened the app and added an end-to-end smoke runner. (1) Security headers: `lib/security/headers.ts` exports `securityHeaders(isProduction)` + `contentSecurityPolicy()`, wired through `next.config.ts` `headers()` for every response — `X-Content-Type-Options`, `X-Frame-Options: DENY`, `Referrer-Policy`, `Permissions-Policy`, `Cross-Origin-Opener-Policy`, a report-only CSP limited to self plus the Google AdSense origins, and HSTS only in production; `poweredByHeader` disabled. (2) Rate limiting: `lib/monetization/rate-limit.ts` (dependency-free fixed-window limiter, env-tunable via `CLICK_RATE_LIMIT_MAX`/`CLICK_RATE_LIMIT_WINDOW_MS`, default 30/60s) now guards the affiliate click tracker, which returns `429` + `Retry-After` per IP hash. (3) Secret-exposure guards: `tests/security/secrets.test.ts` fails the build if `process.env` appears anywhere outside the allowlisted server config modules, if the rendered `app/`/`components/` trees read env directly, if a client component imports a server-only module, or if any `NEXT_PUBLIC_*` variable looks sensitive. (4) `scripts/smoke.ts` (+ `npm run smoke`) runs 18 HTTP end-to-end checks against a running server: public routes, 404, robots/sitemap, the admin guard, tracker 400/404/302, security headers and absence of `X-Powered-By`. (5) New `docs/project-memory/SECURITY.md` documents the posture and the remaining launch work.
- FILES: lib/security/headers.ts, lib/monetization/rate-limit.ts, app/api/track/click/route.ts, next.config.ts, lib/env.ts (`clickRateLimitMax`, `clickRateLimitWindowMs`), .env.example, scripts/smoke.ts, package.json (`smoke`), tests/security/{headers,secrets}.test.ts, tests/monetization/rate-limit.test.ts, docs/project-memory/SECURITY.md
- DATABASE IMPACT: none (no schema change). The tracker still writes `affiliate_clicks`, now rate limited.
- TEST RESULT: typecheck PASS, eslint PASS, vitest 147/147 PASS (11 new security/rate-limit tests), production build PASS (zero warnings). Live verification against the production build: `npm run smoke` 18/18 checks pass; HSTS + report-only CSP + baseline headers present on `/` (HSTS absent in dev); 32 rapid tracked requests from one IP returned 30× `302` then `429`; `X-Powered-By` absent.

## 2026-09-17 — Phase 8 (Monetization)
- CHANGE: Implemented the monetization layer, all configuration-driven and inactive by default. (1) Ads: an env-resolved ad config (`ADSENSE_CLIENT` + per-placement slots) and a server `AdSlot` component rendering a labelled AdSense unit plus the loader script only when both client and slot are configured — unconfigured placements render nothing (no placeholder containers); the public layout carries a header slot, the homepage an in-content slot, the tool detail sidebar a sidebar slot and `/tools` a listings slot. (2) Affiliate tracking: a public `GET /api/track/click?tool=<slug>` endpoint that resolves the destination from the database by slug (published tools only, `http(s)` only) so it can never act as an open redirect, records an `affiliate_clicks` row with a salted SHA-256 IP hash (no raw IP) and truncated user-agent/referrer, then 302s to the affiliate URL (falling back to the tool website). Tool-detail CTAs for tools with an affiliate URL now point at the tracker and render an Arabic affiliate disclosure; other CTAs keep the direct link with `rel="nofollow sponsored noopener"`. (3) Sponsored placements: `listActiveSponsoredTools(placement, limit)` (published tool + `active` campaign + placement + in-window dates) and `getActiveSponsorship(toolId)`; clearly labelled "أدوات مُموَّلة / محتوى مدفوع" blocks on the homepage (`featured`) and `/tools` (`listings`), and a "مُموَّل" badge on the tool page for active campaigns. Dev seed now includes a clearly-labelled demo campaign pointing at `example.com` so the path is verifiable end to end.
- FILES: lib/monetization/ads.ts, components/site/ad-slot.tsx, components/site/affiliate-disclosure.tsx, app/api/track/click/route.ts, lib/db/queries/public.ts (`listActiveSponsoredTools`, `getActiveSponsorship`, `gte`/`lte` import), app/(public)/layout.tsx, app/(public)/page.tsx, app/(public)/tools/page.tsx, app/(public)/tools/[slug]/page.tsx, scripts/seed.ts, tests/monetization/{ads,tracking,sponsored}.test.ts
- DATABASE IMPACT: writes `affiliate_clicks` on each tracked click (tool id, salted IP hash, truncated UA/referrer). Reads `sponsored_listings` joined to published tools. No schema changes.
- TEST RESULT: typecheck PASS, eslint PASS, vitest 136/136 PASS (16 new monetization tests), production build PASS (zero warnings). Live verification against the production build: sponsored and ad slots absent when `ADSENSE_*` is empty; with test ad ids set at build time the header/sidebar slots render with the configured client and the loader script while unset placements emit nothing; `/api/track/click?tool=sample-chat-assistant` → `302` to the stored affiliate URL with the click persisted (hash length 64, UA stored), unknown tool → `404`, missing parameter → `400`; demo campaign renders the labelled sponsored blocks on `/` and `/tools` and the "مُموَّل" badge on the tool page; `/privacy` carries the affiliate disclosure.

## 2026-09-17 — Phase 7 (SEO)
- CHANGE: Added the SEO layer. Shared `buildPageMetadata` (absolute canonical, Open Graph, Twitter card, `noindex` for filtered views and an absolute-title mode for the homepage) built on a normalized site origin; root layout gains `metadataBase`, default title template, keywords, robots/googleBot directives, Open Graph/Twitter defaults and optional Google verification. Per-page metadata + canonical applied to home, `/tools`, tool/category/collection detail, category/collection/gallery lists, `/search` (noindex) and the static pages. JSON-LD builders emit only stored facts: Organization, WebSite (+`SearchAction`), SoftwareApplication (free → price 0 offer; freemium/paid → offer category; unknown → no offers; never a fabricated rating), BreadcrumbList and FAQPage, serialized with `<` escaped. Added `app/sitemap.ts` (static routes + published tools/categories/collections with `lastModified`, falling back to static routes if the DB is unavailable), `app/robots.ts` (allow all; disallow `/admin`, `/api/`, `/search`; absolute sitemap + host) and an Arabic RTL `app/not-found.tsx` 404 page. Internal linking extended: tool pages now list the published collections they belong to.
- FILES: lib/seo/site.ts, lib/seo/metadata.ts, lib/seo/json-ld.ts, components/seo/json-ld.tsx, app/sitemap.ts, app/robots.ts, app/not-found.tsx, app/layout.tsx, app/(public)/page.tsx, app/(public)/tools/page.tsx, app/(public)/tools/[slug]/page.tsx, app/(public)/categories/{page,[slug]/page}.tsx, app/(public)/collections/{page,[slug]/page}.tsx, app/(public)/search/page.tsx, app/(public)/{about,contact,privacy,terms}/page.tsx, lib/db/queries/public.ts (collections per tool), lib/env.ts (`googleSiteVerification`), .env.example, tests/seo/{json-ld,routes}.test.ts
- DATABASE IMPACT: read-only. No schema changes.
- TEST RESULT: typecheck PASS, eslint PASS, vitest 120/120 PASS (17 new SEO tests), production build PASS (zero warnings). Live verification against the production build: `/robots.txt` disallow rules + sitemap pointer, `/sitemap.xml` 200 with `<lastmod>` and published-only URLs, canonical/OG tags, `SoftwareApplication`/`BreadcrumbList`/`FAQPage` JSON-LD on tool pages, `WebSite`+`Organization` on home, filtered `/tools` canonicalized to `/tools` with `noindex, follow`, Arabic 404 with `noindex, nofollow`.

## 2026-09-17 — Phase 6 (Public frontend)
- CHANGE: Built the Arabic-first (RTL) public site under the `app/(public)` route group: shared header/nav/search/category chips/footer layout; homepage (hero, featured tools, latest tools, categories, curated collections); `/tools` directory with keyword/category/pricing/featured/sort filters plus pagination; PG full-text `/search`; tool detail page (descriptions, features, tags, FAQ, updates, related tools, affiliate-aware outbound CTA with `rel="nofollow sponsored noopener"`); category and collection landing pages; static about/contact/privacy/terms. Added a public query layer with raw tsvector/`websearch_to_tsquery` fragments and relevance ranking, plus reusable `ToolCard`/`ToolGrid`/`Pagination` components. Rendering is ISR via `generateStaticParams` for all detail/landing routes (graceful fallback to on-demand if the DB is unavailable at build time); `/tools` and `/search` stay dynamic because they read `searchParams`. Dev seed now publishes two clearly-marked sample tools and one collection so the public site is verifiable end to end.
- FILES: lib/db/queries/public.ts, components/site/tool-card.tsx, components/site/pagination.tsx, app/(public)/layout.tsx, app/(public)/page.tsx, app/(public)/tools/page.tsx, app/(public)/tools/[slug]/page.tsx, app/(public)/categories/page.tsx, app/(public)/categories/[slug]/page.tsx, app/(public)/collections/page.tsx, app/(public)/collections/[slug]/page.tsx, app/(public)/search/page.tsx, app/(public)/{about,contact,privacy,terms}/page.tsx, scripts/seed.ts, app/page.tsx (removed)
- DATABASE IMPACT: read-only for public traffic. Seed now sets two sample tools to `published` (+ `publishedAt`, SEO fields, FAQ JSON, quality score, featured) and the sample collection to `published`. No schema changes.
- TEST RESULT: typecheck PASS, eslint PASS, vitest 103/103 PASS, production build PASS (zero warnings). Route table confirms SSG+ISR (●) for tool/category/collection detail and static (○) for home/landings/static pages. Live checks: all public routes 200; detail/landing pages served with `x-nextjs-cache: HIT` and expected `s-maxage`; unknown slugs → 404.

## 2026-09-17 — Phase 5 (Auth + Admin backend)
- CHANGE: Implemented the secured admin backend: NextAuth v5 credentials auth (bcrypt, JWT, 8h sessions, inactive/short-password rejection, lastLoginAt update), an edge-safe `proxy.ts` optimistic session-cookie guard for `/admin/:path*`, a single authorization choke point (`requireActor`/`requireAdmin`), a testable service layer (`lib/services/*`), thin auth-guarded server actions, and the full admin UI (login, dashboard, tools review queue + detail with status transitions/reprocess/monetization, categories, tags, collections with ordered curation, sources with run-now/toggle, ingestion runs, sponsored campaigns).
- FILES: lib/auth.ts, lib/auth/authorization.ts, types/next-auth.d.ts, app/api/auth/[...nextauth]/route.ts, proxy.ts, lib/db/queries/{admin,catalog}.ts, lib/services/{review,catalog,monetization}.ts, lib/actions/{auth,tools,ingestion,catalog,monetization}.ts, lib/ui/labels.ts, app/admin/login/*, app/admin/(dashboard)/*, app/layout.tsx, app/globals.css, ingestion/pipeline.ts (isUniqueViolation now shared), lib/db/errors.ts, tests/auth/authorization.test.ts, tests/actions/authorization.test.ts, tests/services/{review,catalog,monetization}.test.ts
- DATABASE IMPACT: writes content_revisions for status/field/category/collection/tag/monetization changes; updates tools (status/published_at/is_featured/affiliate_url/fields), categories, tags, collections, collection_tools, sponsored_listings. No schema changes.
- TEST RESULT: typecheck PASS, eslint PASS, vitest 103/103 PASS (43 new). Production build PASS (all admin routes dynamic; no Turbopack warnings). Live runtime verification: anon `/admin*` → 307 to login with callbackUrl; credentials login sets `authjs.session-token`; all admin pages return 200; wrong password rejected; unknown tool → 404.

## 2026-09-17 — Phase 4 (Gemini AI layer)
- CHANGE: Implemented the bilingual (Arabic-first) AI enrichment layer: provider abstraction (Gemini live + injectable scripted providers), lenient JSON parsing, strict Zod output contract, anti-hallucination fact checks (unknown URLs, unknown categories, missing evidence, low confidence), deterministic 0-100 quality scorer, publication gate, transactional persistence (tool fields, tags/features upsert, content revision, ai_processing_logs), retry with backoff for transient provider errors, and worker wiring that runs ingestion-only when no API key is present.
- FILES: lib/ai/provider.ts, lib/ai/json.ts, lib/ai/schemas.ts, lib/ai/prompts.ts, lib/ai/fact-check.ts, lib/ai/quality.ts, lib/ai/enrich.ts, ingestion/pipeline.ts (EnrichArgs gains ingestionItemId), scripts/worker.ts, tests/ai/{fixtures,quality,provider,schemas,prompts,enrich}.test.ts
- DATABASE IMPACT: tools (descriptions/SEO/pricing/faq/category/quality_score/status/published_at), tags + tool_tags, features + tool_features, content_revisions (reason ai_create), ai_processing_logs. No schema changes.
- TEST RESULT: typecheck PASS, eslint PASS, vitest 60/60 PASS (29 new AI tests). Worker live run confirms AI-disabled path is reported clearly; Gemini live call PENDING (no GEMINI_API_KEY on this machine).

## 2026-09-17 — Phase 3 (Ingestion engine)
- CHANGE: Implemented the adapter-based, idempotent ingestion engine: HTTP client with timeout/error classification, RSS/Atom adapter (rss-parser, defaultRSS 2.0), offline fixture adapter, adapter registry, normalization + Zod validation, duplicate detection, transactional per-item pipeline with run/item metrics, multi-source orchestrator, and CLI worker.
- FILES: ingestion/types.ts, ingestion/errors.ts, ingestion/normalize.ts, ingestion/dedupe.ts, ingestion/pipeline.ts, ingestion/run.ts, ingestion/registry.ts, ingestion/adapters/http.ts, ingestion/adapters/rss.ts, ingestion/adapters/fixture.ts, lib/validation/ingestion.ts, lib/utils/text.ts, lib/utils/url.ts, lib/utils/hash.ts, scripts/worker.ts, scripts/seed.ts, data/fixtures/sample-feed.xml, tests/ingestion/normalize.test.ts, tests/ingestion/rss-adapter.test.ts, tests/ingestion/pipeline.test.ts, tests/fixtures/rss/*
- DATABASE IMPACT: writes tools (draft), tool_sources (idempotency anchor), ingestion_runs, ingestion_items; updates sources.last_fetched_at/last_success_at. No schema changes.
- TEST RESULT: typecheck PASS, eslint PASS, vitest 31/31 PASS (20 new ingestion/unit + 11 DB). Worker live run against aidiscovery_dev: run 1 created 2, run 2 duplicates 2 (idempotent); no network used (fixture adapter).

## 2026-09-17 — Phase 2 (Database)
- CHANGE: Implemented and verified the full PostgreSQL/Drizzle data layer, migration pipeline, seeds, and DB test harness.
- FILES: lib/db/schema.ts, lib/db/db.ts, lib/db/migrations/0000_daily_tarantula.sql, drizzle.config.ts, lib/env.ts, scripts/migrate.ts, scripts/seed.ts, scripts/seed-admin.ts, tests/global-setup.ts, tests/db/helpers.ts, tests/db/schema.test.ts, vitest.config.ts, package.json, .env.development, .env.test
- DATABASE IMPACT: 19 tables, 11 enums, 47 indexes (incl. GIN trigram + tsvector), pg_trgm extension; applied to aidiscovery_dev and aidiscovery_test.
- TEST RESULT: typecheck PASS, eslint PASS, vitest 11/11 PASS; seeds idempotent (double-run verified).

## 2026-09-17 — Phase 1 (Architecture)
- CHANGE: Bootstrapped project-memory system and architecture documentation.
- FILES: docs/project-memory/*.md
- DATABASE IMPACT: none
- TEST RESULT: n/a (no code yet)